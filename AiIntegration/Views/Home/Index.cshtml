@{
    ViewData["Title"] = "Home Page";
}

<form class="form-floating" asp-action="Index" method="post">
    <input type="text" class="form-control" id="floatingInputValue" placeholder="Ask your question" name="question">
    <label for="floatingInputValue">The AI knows (mostly) everything—except when it's spinning tall tales.</label>
    <button type="submit" class="btn btn-primary">Sucker Button</button>
</form>
@if (ViewBag.Answer != null)
{
    <div class="alert alert-primary" role="alert">
        @ViewBag.Answer
    </div>
}
